Given:
m : row_of_matrix
n : col_of_matrix
vector<vector<int>>indices : [ri, ci] that will +1 for each row or col element
each cell default value is 0

Return:
design a function returning the number that cell's value is odd in matrix

Example:
given:
m = 2
n = 3
indices = [[0,1], [1,1]]

matrix:
[[0,0,0], [0,0,0]] -> [[1,2,1], [0,1,1]] -> [[1,3,1], [1,3,1]]
odd_cell = 6
so the function should return 6.